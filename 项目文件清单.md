# 📦 项目文件清单

## 📁 源代码文件 (src/)

### 🎨 样式文件
```
src/app/globals.css          # 全局样式 + 主题系统 + 礼簿样式
```

### 📄 页面组件 (Pages)
```
src/app/
├── page.tsx                 # 首页 - 自动路由跳转
├── layout.tsx               # 根布局
├── not-found.tsx            # 404 页面
│
├── setup/
│   └── page.tsx             # 创建事项页面
│
├── main/
│   └── page.tsx             # 主界面（录入+展示）
│
├── guest-screen/
│   └── page.tsx             # 副屏展示
│
└── test-redirect/
    └── page.tsx             # 路由测试工具
```

### 🔧 工具库
```
src/lib/
├── crypto.ts                # AES-256 加密服务
├── utils.ts                 # 工具函数（格式化、转换）
└── github.ts                # GitHub API 封装
```

### 📦 类型定义
```
src/types/
└── index.ts                 # Event, GiftData, GiftRecord 等类型
```

---

## 📄 配置文件

```
package.json                 # 依赖和脚本
tsconfig.json                # TypeScript 配置
tailwind.config.ts           # Tailwind CSS 配置
next.config.ts               # Next.js 配置
postcss.config.js            # PostCSS 配置
.gitignore                   # Git 忽略文件
next-env.d.ts                # Next.js 类型定义
```

---

## 📚 文档文件

### 核心文档
```
README.md                    # 项目介绍
使用指南.md                  # 完整使用说明
完成总结.md                  # 项目完成总结
```

### 技术文档
```
STYLES.md                    # 样式系统说明
FIX_REDIRECT.md              # 路由跳转修复方案
修复跳转问题.md.md           # 中文修复指南
```

### 快速参考
```
QUICK_REFERENCE.md           # 快速参考
START.md                     # 启动说明
INSTALL.md                   # 安装说明
STRUCTURE.md                 # 项目结构
PROJECT_SUMMARY.md           # 项目概要
FILE_LIST.txt                # 文件列表
```

### 启动脚本
```
start.sh                     # Linux/Mac 启动脚本
start.bat                    # Windows 启动脚本
```

---

## 🗂️ 其他目录

```
copy/                        # 原始 HTML 版本（参考）
.next/                       # Next.js 构建输出（自动生成）
node_modules/                # 依赖包（自动生成）
```

---

## 📊 文件统计

### 源代码
- **TypeScript/React 组件**: 7 个页面 + 3 个工具库
- **样式文件**: 1 个全局 CSS
- **类型定义**: 1 个类型文件

### 文档
- **中文文档**: 5 个
- **英文文档**: 6 个
- **配置文件**: 5 个

### 总计
- **代码文件**: ~11 个
- **文档文件**: ~11 个
- **配置文件**: 5 个

---

## 🔍 关键文件说明

### 1. `src/app/globals.css`
**作用**: 全局样式 + 主题系统
**包含**:
- 喜庆红/肃穆灰主题变量
- 礼簿框架样式
- 按钮、输入框组件样式
- 响应式设计
- 打印样式

### 2. `src/app/page.tsx`
**作用**: 首页路由跳转逻辑
**功能**:
- 检查跳转标记
- 根据 localStorage 决定跳转目标
- 防止无限循环

### 3. `src/lib/crypto.ts`
**作用**: AES-256 加密服务
**功能**:
- 数据加密
- 数据解密
- 密码哈希（SHA-256）

### 4. `src/lib/github.ts`
**作用**: GitHub API 封装
**功能**:
- 读取文件
- 写入文件
- 测试连接
- 同步数据

### 5. `src/app/main/page.tsx`
**作用**: 主界面（核心功能）
**功能**:
- 礼金录入表单
- 礼簿网格展示
- 分页导航
- 数据统计
- 导出功能

---

## 🎯 文件依赖关系

```
globals.css
  └─ 所有页面都使用

crypto.ts
  └─ setup/page.tsx (加密事件)
  └─ main/page.tsx (加密/解密礼金)

utils.ts
  └─ setup/page.tsx (格式化时间)
  └─ main/page.tsx (格式化金额、中文大写)
  └─ guest-screen/page.tsx (中文大写)

github.ts
  └─ setup/page.tsx (同步事件)

types/index.ts
  └─ 所有组件都使用
```

---

## 🚀 快速定位

### 修改样式
→ 编辑 `src/app/globals.css`

### 修改路由逻辑
→ 编辑 `src/app/page.tsx`

### 修改加密方式
→ 编辑 `src/lib/crypto.ts`

### 修改 GitHub 同步
→ 编辑 `src/lib/github.ts`

### 修改主界面
→ 编辑 `src/app/main/page.tsx`

### 添加新页面
→ 在 `src/app/` 下创建新文件夹和 `page.tsx`

---

## ✅ 完整文件树

```
gift-list/
├── src/
│   ├── app/
│   │   ├── globals.css              # 全局样式
│   │   ├── layout.tsx               # 根布局
│   │   ├── not-found.tsx            # 404 页面
│   │   ├── page.tsx                 # 首页
│   │   ├── setup/
│   │   │   └── page.tsx             # 创建页面
│   │   ├── main/
│   │   │   └── page.tsx             # 主界面
│   │   ├── guest-screen/
│   │   │   └── page.tsx             # 副屏
│   │   └── test-redirect/
│   │       └── page.tsx             # 测试工具
│   ├── lib/
│   │   ├── crypto.ts                # 加密服务
│   │   ├── utils.ts                 # 工具函数
│   │   └── github.ts                # GitHub API
│   ├── types/
│   │   └── index.ts                 # 类型定义
│   └── components/                  # （预留）
│
├── 配置文件/
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── next.config.ts
│   └── postcss.config.js
│
├── 文档/
│   ├── README.md
│   ├── 使用指南.md
│   ├── 完成总结.md
│   ├── STYLES.md
│   ├── FIX_REDIRECT.md
│   ├── 修复跳转问题.md
│   └── 项目文件清单.md  ← 当前文件
│
├── 其他/
│   ├── copy/                    # 参考实现
│   ├── start.sh                 # 启动脚本
│   ├── start.bat                # Windows 启动脚本
│   └── .gitignore
│
└── 自动生成/
    ├── .next/                   # 构建输出
    ├── node_modules/            # 依赖包
    └── pnpm-lock.yaml           # 依赖锁定
```

---

**文件清单创建完成！** 📋

所有文件都已就绪，可以开始开发或使用了！
